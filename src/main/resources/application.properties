# Puerto del backend (coincide con docker-compose)
server.address=0.0.0.0
server.port=8081
# Conexión a la base de datos MySQL (dentro de Docker)
spring.datasource.url=jdbc:mysql://mysql-db:3306/appnube6?useSSL=false&serverTimezone=UTC
spring.datasource.username=appuser
spring.datasource.password=app1234

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
